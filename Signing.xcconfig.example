// Committed signing defaults. `xcodegen generate` writes Signing.xcconfig
// from .env.local when APPLE_SIGNING_IDENTITY or PEEKBAR_SIGNING_IDENTITY is set.
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY = Apple Development
DEVELOPMENT_TEAM =

#include? "Signing.xcconfig"
